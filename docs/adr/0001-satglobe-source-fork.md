# ADR 0001: KeepTrack source fork and SatGlobe product boundary

- Status: accepted
- Date: 2026-07-15
- Upstream baseline: KeepTrack v13.4.0 (`f06b30bd`)

## Context

SatGlobe needs KeepTrack’s complete WebGL renderer, propagation workers, catalog enrichment, time model, camera, orbit paths, and color systems. KeepTrack’s source release is ahead of its reusable npm surface, and the required engine seams are not exposed as a stable package API.

The application must run locally without accounts, a backend, runtime catalogs, analytics, or remote story assets. It must remain type-safe and able to absorb later KeepTrack releases without scattering product behavior through upstream code.

## Decision

SatGlobe is maintained as an AGPL-3.0 source fork. Upstream history and tags remain intact and the Git remote named `upstream` tracks KeepTrack.

All SatGlobe-owned product code lives under `src/satglobe`. Only `src/satglobe/engine` may import KeepTrack or OOTK internals. React renders into a sibling overlay above KeepTrack’s canvas; the `satglobe` profile suppresses the upstream visual shell but retains engine services. `src/main.ts` contains the minimal edition-specific mount and service-worker exclusion.

The browser reads only bundled resources. Catalog refresh is a manual Node command that validates all inputs before atomically replacing the checked-in catalog. Portable view JSON is strict, versioned data and cannot resolve scripts or arbitrary remote assets.

## Consequences

- Upstream engine capability is retained without reimplementing propagation or rendering.
- SatGlobe remains covered by AGPL-3.0. If it is offered over a network, users must receive access to the corresponding modified source as required by the license.
- The adapter is intentionally narrow and is the expected conflict-resolution point during upstream sync.
- The React version and schema library become additional maintained dependencies.
- Browser runtime behavior can be tested with the network disabled; a catalog update requires deliberate local operator action.
- KeepTrack Pro is not part of SatGlobe and is not required to install, test, or build it.

## Upstream policy

1. Fetch release tags from `upstream`.
2. Merge a selected tag into a dedicated `codex/upstream-keeptrack-*` branch.
3. Preserve upstream changes unless SatGlobe’s documented profile or adapter boundary requires a small modification.
4. Run typecheck, SatGlobe unit tests, production build, offline E2E, and selected upstream tests.
5. Record any unavoidable upstream-file modification in this ADR or a successor.

## Upstream modification log

- 2026-07-17 — `src/locales/locales.ts`, `src/locales/__tests__/locales.test.ts`, `src/main.ts`, `build/webpack-manager.ts`: kept English in the entry bundle and moved the 11 non-English translation resources behind an i18next backend with explicit same-origin dynamic imports. The main entry starts catalog prefetch immediately, then awaits the detected locale before engine/UI initialization, preserving both fetch overlap and ready-before-UI behavior; default English boot avoids all locale chunk requests, and a failed selected-locale chunk explicitly switches to bundled English before boot continues. The SatGlobe production entry fell from 7.51 MiB to 5.15 MiB, so ADR 0002's enforced ceiling was ratcheted from 8 MiB to 6.5 MiB.
- 2026-07-17 — `test/weatherApi/weather.test.ts` (+ regenerated snapshot): replaced the machine-dependent full-object snapshot of `calculatePasses_()` with a stable projection (sensor `objName`, pass type, ISO pass windows). The raw `DetailedSensor` graph embeds trig-derived ECF positions that differ at ~1e-12 across libm implementations, which made the upstream snapshot fail on some machines (previously noted in `SATGLOBE.md`).
- 2026-07-17 — `build/webpack-manager.ts`: removed the asset-size `ignoreWarnings` suppression and added an ADR 0002 bundle budget (`performance.hints: 'error'`, 8 MiB per JS asset/entrypoint, JS-only filter, `COVERAGE=1` exempt) to production builds.
- 2026-07-17 — `src/plugins/orbit-guard-menu/orbit-guard-menu.ts`, `src/plugins/sat-info-box-orbit-guard/sat-info-box-orbit-guard.ts`: removed the hardcoded OrbitGuard bearer token and plaintext-HTTP raw-IP endpoint (a committed credential); the constants are now empty and both fetch paths no-op with a logged notice until an operator supplies deployment values. Also deleted the stale Pro-only `auth/callback.html` artifact, which the satglobe edition never builds.
- 2026-07-17 — `src/engine/rendering/color-scheme-manager.ts`: added a public `registerScheme(scheme)` method so addon color schemes (e.g. SatGlobe's) register through a typed seam instead of casting into the `colorSchemeInstances` literal. Also the expected seam for later worker-support registration.
- 2026-07-17 — `src/webworker/positionCruncher.ts`: `sendDataToSatSet` now posts worker-side copies of `satPos`/`satVel` (and per-sensor view/sun arrays) with transfer lists instead of structured-cloning the live reusable buffers, moving the per-cycle serialization cost off the main thread (resolves the file's SharedArrayBuffer TODO with the same double-buffer approach every other worker uses).
- 2026-07-17 — `src/engine/rendering/color-schemes/color-scheme.ts` + `color-scheme-manager.ts`: added an opt-in `isStaticColorScheme` contract — schemes whose colors depend only on catalog + explicitly applied state skip the periodic per-frame progressive recolor (selection/hover overlays repaint from the clean snapshot). Added a CPU-side dirty flag so `sendColorBufferToGpu` performs zero `bufferSubData` calls while colorData/pickableData are unchanged; every CPU write site marks the flag. Position uploads are intentionally untouched (positions interpolate on the CPU every frame).
- 2026-07-17 — `src/app/ui/search-manager.ts`: search-result rows now escape every catalog-derived field (names, designators, bus, launch vehicle, missile descriptions) through the existing `escapeHtml` helper before `innerHTML` insertion, closing the stored-XSS path between external catalog sources and the DOM. Match highlighting preserved via an escaped three-segment helper.
- 2026-07-17 — repo-wide (F4, ESLint 9 flat config): folded the three legacy eslintrc files into `eslint.config.mjs`. Upstream source edits limited to comments and one test: pruned 349 unused inline `eslint-disable` directives across ~190 upstream files (flat config reports dead directives; `--fix` removal, no code changes), renamed two stale disables from the removed core `require-jsdoc` to `jsdoc/require-jsdoc`, widened the JWST feature-off switch's disable in `src/app/rendering/mesh/model-resolver.ts` to also cover the new `no-constant-binary-expression`, and made `src/app/ui/__tests__/ui-validation.test.ts` deterministic by setting `KeepTrack.getInstance().containerRoot` in `beforeEach` (it previously relied on ambient state and failed on machines where nothing had assigned it; sibling tests already use this pattern).
- 2026-07-17 — `src/app/data/catalog-loader.ts`, `src/keeptrack.ts`: added `performance.mark`/`measure` boot-phase instrumentation (`catalog:fetch+decode`, `catalog:build`, `engine:ready`) so startup work is attributable in DevTools and benchmarks without a tracing build. No behavioral change.
- 2026-07-17 — `src/main.ts`, `src/app/data/catalog-loader.ts`, `src/global.d.ts`: the offline edition starts the bundled-catalog fetch at module evaluation (`window.satGlobeCatalogPrefetch`, gated on the compile-time `__EDITION__` constant so other editions dead-code-eliminate it); CatalogLoader's offlineMode branch consumes the promise and falls back to its own fetch on failure. Overlaps the 19 MB fetch+decode with engine init instead of serializing after it.
- 2026-07-17 — `src/engine/rendering/gl-utils.ts`, `src/locales/locales.ts`, `build/dev-server.ts`: texture uploads of 8k+ bitmaps now stream in ~16 MiB row bands with yields between bands (dormant at satglobe's 2k/4k defaults; guards the 16k qualities upstream settings offer); per-texture upload/mipgen performance marks added; the 2,900-key translation warm-up chunks against the idle deadline instead of one synchronous callback; the satglobe dev server sends `Document-Policy: js-profiling` so local perf work can sample real stacks.
- 2026-07-17 — `src/engine/ootk` vendored into the tree (was a git submodule of thkruz/ootk @ b9dbc5be). The submodule made engine-level changes unlandable from this repo (superproject commits cannot carry submodule edits — PR #35 shipped without its code). Vendored: `src/`, `LICENSE.MD`, `README.md`, `package.json` under AGPL with headers intact; upstream's own tests/benchmarks/docs/tooling (46 of 51 MB) pruned — this repo's vitest never ran them. CI's four submodule-init steps removed; `build/get-submodules.ts` reads `.gitmodules` dynamically and now handles only `src/plugins-pro`. First vendored change: lazy `satrec` creation (boot catalog block 1,095 ms → 564 ms measured).
- 2026-07-17 — `src/app/ui/hover-manager.ts`, `src/app/sensors/sensorManager.ts`: catalog-derived strings (satellite/missile names, country, sensor system/frequency band) now pass through `escapeHtml` before innerHTML interpolation, matching the search-manager fix; hostile-name regression test added. `scripts/mesh-viewer/server.ts` binds loopback only. `src/engine/ootk/package.json` stripped to an inert marker (its exports pointed at a dist/ that was never vendored).
- 2026-07-17 — `src/app/data/catalog-loader.ts` (`replaceAll('\r')` instead of first-occurrence replace; celestrak provenance check parses the URL hostname instead of substring matching), `src/plugins/calculator/calculator.ts` (input defaults escaped before innerHTML attribute interpolation): closes the remaining CodeQL findings with code fixes rather than dismissals.
- 2026-07-18 — `public/index.html`, `public/service-worker-bootstrap.js`, `build/dev-server.ts`, `build/dev-server-response.ts`, `build/__tests__/dev-server.test.ts`, `configs/satglobe/nginx.conf`, `test/vitest-setup.ts`: externalized the upstream early service-worker update bootstrap as a deferred same-origin asset, preserving its before-main execution order while satisfying SatGlobe's strict `script-src` without `unsafe-inline` or a minifier-sensitive CSP hash. Nginx revalidates that stable bootstrap filename instead of caching it immutably. Static serving no longer injects or exposes development reload/plugin capabilities; development loads its reload client from a CSP-compatible same-origin endpoint and again watches profile configuration changes. The local server binds to IPv4 loopback and confines decoded request paths to `dist/`, with live regressions for encoded traversal and static capability denial. Focused tests keep the local SatGlobe and nginx CSP directives identical. The shared JSDOM harness no longer attempts public-origin external-template loads whose asynchronous resource errors can outlive a Vitest worker.
- 2026-07-18 — `build/dev-server.ts`, `build/__tests__/dev-server.test.ts`, `build/webpack-manager.ts`, `build/lib/build-provenance.ts`, `build/lib/fixed-executables.ts`, `scripts/plugin/lib/git.ts`, `scripts/satglobe/verify-stories.ts`, `scripts/satglobe/docker-build.mjs`, `Dockerfile.satglobe`, `.dockerignore`: canonicalized static files before enforcing the `dist/` boundary so an in-tree symlink cannot escape it, and made the story audit compare the complete shared production CSP rather than a permissive substring. Git subprocesses reachable from the production audit now use fixed locations instead of caller-controlled `PATH`. The Docker build installs Git at `/usr/bin/git`; its plain-Node fixed-path host wrapper requires a clean worktree and passes the validated full commit SHA so linked-worktree `.git` pointers never leak into image provenance. Docker excludes environment files, local dependencies, build output, coverage, audit artifacts, and Git metadata. Local and container builds derive the same 12-character display hash from a validated full object ID. The walker invokes the three local TypeScript build steps directly through the current absolute Node executable and preserves the original monotonic five-minute aggregate deadline.
- 2026-07-18 — `package.json`: registered the production story walker, its lint/type gate, and the bundled conjunction-refresh test in the normal SatGlobe verification scripts so those Wave-1 audit paths remain versioned and repeatable; routed `docker:satglobe` through the clean-worktree provenance wrapper instead of invoking `docker build` directly.
- 2026-07-18 — `playwright.config.ts`: moved disposable Playwright traces, screenshots, and videos into `test-results/playwright` so browser retries cannot collide with durable story-walker evidence; Packet 5 also made CI reject any test that passes only on retry after the camera assertion was moved to the actual mode/target seam.
- 2026-07-18 — `vitest.config.ts`: ratcheted the full-source coverage floors from 71/57/73/71 to 72/58/74/72 for statements/branches/functions/lines against the post-Wave-1 measured baseline, without narrowing the denominator.
- 2026-07-18 — `.github/dependabot.yml`: removed the inherited `develop` targets, allowing both npm and GitHub Actions updates to follow this fork's actual default branch (`main`) instead of a nonexistent branch.
- 2026-07-18 — `src/app/analysis/sat-math.ts`, `src/app/data/catalog-manager/satLinkManager.ts`, `src/engine/math/dop-math.ts`, `src/engine/math/linear-algebra.ts`, `src/keepTrackApi.ts`, `configs/satglobe/nginx.conf`, `package.json`, `package-lock.json`: replaced the three imports of eval-generating `numeric` with a typed three-vector dot product and a double-precision 4×4 Gauss-Jordan inverse with partial pivoting, then removed `numeric` and `@types/numeric`. Valid SatMath, crosslink-angle, and DOP results retain their established numerical outputs; singular or non-finite DOP geometry now returns the existing `50.00` sentinel instead of throwing or exposing `NaN`, and invalid crosslink positions no longer draw a line. The CSP still retains `unsafe-eval` for the optional Emscripten SGP4 WASM glue until that backend is gated separately.
